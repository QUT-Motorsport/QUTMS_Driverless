channels:
  - robostack-experimental
  - conda-forge
dependencies:
  # ---------------------------------------------------------------------------
  # ROS Base Env deps
  # ---------------------------------------------------------------------------
  - python=3.8
  - ros-galactic-desktop
  - ca-certificates
  - openssl
  - compilers
  - cmake
  - pkg-config
  - make
  - ninja
  - colcon-common-extensions
  - pip
  - pip:
    # colcon shell support
    - colcon-argcomplete
    - colcon-bash
    - colcon-zsh
